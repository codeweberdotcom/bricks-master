# Руководство по Gulp

Это подробное руководство описывает систему сборки Gulp для темы Codeweber.

## 📋 Содержание

1. [Обзор системы](#обзор-системы)
2. [Установка и настройка](#установка-и-настройка)
3. [Структура проекта](#структура-проекта)
4. [Задачи (Tasks)](#задачи-tasks)
5. [Работа с дочерними темами](#работа-с-дочерними-темами)
6. [Команды](#команды)
7. [Настройки и конфигурация](#настройки-и-конфигурация)
8. [Обработка файлов](#обработка-файлов)
9. [Отладка и решение проблем](#отладка-и-решение-проблем)

## 🎯 Обзор системы

Gulp используется для автоматизации задач разработки:

- ✅ Компиляция SCSS в CSS
- ✅ Минификация и оптимизация файлов
- ✅ Обработка изображений
- ✅ Сборка JavaScript файлов
- ✅ Поддержка дочерних тем
- ✅ Автоматическая перезагрузка браузера (BrowserSync)
- ✅ Source maps для отладки

### Основные файлы:

- **`gulpfile.js`** - Конфигурация Gulp
- **`package.json`** - Зависимости проекта
- **`src/`** - Исходные файлы
- **`dist/`** - Скомпилированные файлы для продакшена
- **`dev/`** - Скомпилированные файлы для разработки

## 🔧 Установка и настройка

### Требования

- **Node.js** версии 14 или выше
- **npm** (устанавливается вместе с Node.js)
- **Gulp CLI** (глобально)

### Установка Gulp CLI глобально

```bash
npm install -g gulp-cli
```

### Установка зависимостей

```bash
cd wp-content/themes/codeweber
npm install
```

Это установит все зависимости из `package.json`:
- gulp и плагины
- sass компилятор
- browser-sync
- imagemin и оптимизаторы изображений
- и другие инструменты

### Проверка установки

```bash
gulp --version
```

Должна отобразиться версия Gulp CLI.

## 📁 Структура проекта

### Исходные файлы (src/)

```
src/
├── assets/
│   ├── scss/              # SCSS стили
│   │   ├── style.scss     # Главный файл стилей
│   │   ├── _variables.scss
│   │   ├── _user-variables.scss  # Пользовательские переменные
│   │   ├── colors/        # Цветовые схемы
│   │   ├── fonts/         # Стили шрифтов
│   │   └── theme/         # Компоненты темы
│   ├── js/                # JavaScript файлы
│   │   ├── theme.js       # Основной JS файл темы
│   │   ├── restapi.js     # REST API клиент
│   │   ├── ajax-filter.js # AJAX фильтр
│   │   ├── ajax-download.js # AJAX загрузка
│   │   ├── testimonial-form.js # Форма отзывов
│   │   └── vendor/        # Сторонние библиотеки
│   ├── img/               # Изображения
│   ├── fonts/             # Шрифты
│   ├── media/             # Видео и медиа файлы
│   ├── php/               # PHP файлы (контакты, PHPMailer)
│   └── css/
│       └── vendor/        # Сторонние CSS
└── partials/              # HTML части для включения
```

### Результат сборки (dist/)

```
dist/
├── assets/
│   ├── css/              # Скомпилированные CSS
│   │   ├── style.css     # Основной файл стилей
│   │   ├── style.css.map # Source map
│   │   ├── plugins.css   # Собранные vendor стили
│   │   └── fonts/        # Стили шрифтов
│   ├── js/               # JavaScript файлы
│   │   ├── theme.js
│   │   ├── plugins.js    # Собранные vendor скрипты
│   │   └── ...
│   ├── img/              # Оптимизированные изображения
│   ├── fonts/            # Шрифты
│   └── media/            # Медиа файлы
```

## ⚙️ Задачи (Tasks)

### Основные задачи

#### `gulp build:dist`

Полная сборка для продакшена:

```bash
gulp build:dist
```

**Что делает:**
- Очищает папку `dist/`
- Компилирует SCSS в CSS с минификацией
- Собирает и минифицирует JavaScript
- Оптимизирует изображения
- Копирует шрифты и медиа файлы
- Создает source maps

#### `gulp build:dev`

Сборка для разработки:

```bash
gulp build:dev
```

**Что делает:**
- Очищает папку `dev/`
- Компилирует SCSS без минификации (читаемый код)
- Копирует JavaScript без минификации
- Оптимизирует изображения
- Копирует все ресурсы

#### `gulp serve`

Запуск сервера разработки с автоперезагрузкой:

```bash
gulp serve
```

**Что делает:**
- Запускает BrowserSync сервер
- Открывает браузер на `http://localhost:3000`
- Следит за изменениями файлов
- Автоматически перезагружает браузер

#### `gulp default` или `gulp`

Запускает полную сборку и сервер:

```bash
gulp
# или
gulp default
```

**Что делает:**
- Выполняет `build:dist`
- Запускает `webserver` и `watch`

### Задачи для отдельных типов файлов

#### CSS задачи

**`css:dev`** - Компиляция SCSS для разработки
```bash
gulp css:dev
```

**`css:dist`** - Компиляция SCSS для продакшена
```bash
gulp css:dist
```

**Особенности:**
- Компиляция SCSS в CSS
- Автопрефиксер для кроссбраузерности
- Минификация для dist
- Source maps для dist
- Unicode поддержка

#### JavaScript задачи

**`themejs:dev`** - Копирование theme.js для разработки
```bash
gulp themejs:dev
```

**`themejs:dist`** - Копирование theme.js для продакшена
```bash
gulp themejs:dist
```

**`pluginsjs:dev`** - Сборка vendor JavaScript для разработки
```bash
gulp pluginsjs:dev
```

**`pluginsjs:dist`** - Сборка и минификация vendor JavaScript
```bash
gulp pluginsjs:dist
```

**Отдельные JS модули:**
- `restapijs:dev` / `restapijs:dist`
- `testimonialformjs:dev` / `testimonialformjs:dist`
- `ajaxdownloadjs:dev` / `ajaxdownloadjs:dist`
- `ajaxfilterjs:dev` / `ajaxfilterjs:dist`

#### Изображения

**`image:dev`** - Оптимизация изображений для разработки
```bash
gulp image:dev
```

**`image:dist`** - Оптимизация изображений для продакшена
```bash
gulp image:dist
```

**Что делает:**
- Оптимизация JPEG (качество 80-90%)
- Оптимизация PNG (pngquant)
- Оптимизация GIF (gifsicle)
- Оптимизация SVG (svgo)

#### Шрифты

**`fonts:dev`** - Копирование шрифтов для разработки
```bash
gulp fonts:dev
```

**`fonts:dist`** - Копирование шрифтов для продакшена
```bash
gulp fonts:dist
```

**`fontcss:dev`** - Компиляция стилей шрифтов для разработки
```bash
gulp fontcss:dev
```

**`fontcss:dist`** - Компиляция стилей шрифтов для продакшена
```bash
gulp fontcss:dist
```

#### Цветовые схемы

**`colorcss:dev`** - Компиляция цветовых схем для разработки
```bash
gulp colorcss:dev
```

**`colorcss:dist`** - Компиляция цветовых схем для продакшена
```bash
gulp colorcss:dist
```

#### Vendor CSS

**`vendorcss:dev`** - Сборка vendor CSS для разработки
```bash
gulp vendorcss:dev
```

**`vendorcss:dist`** - Сборка и минификация vendor CSS
```bash
gulp vendorcss:dist
```

#### Медиа файлы

**`media:dev`** - Копирование медиа файлов для разработки
```bash
gulp media:dev
```

**`media:dist`** - Копирование медиа файлов для продакшена
```bash
gulp media:dist
```

#### PHP файлы

**`php:dev`** - Копирование PHP файлов для разработки
```bash
gulp php:dev
```

**`php:dist`** - Копирование PHP файлов для продакшена
```bash
gulp php:dist
```

#### HTML (закомментировано)

HTML задачи закомментированы в gulpfile.js, но доступны:

**`html:dev`** - Компиляция HTML для разработки
```bash
gulp html:dev
```

**`html:dist`** - Компиляция HTML для продакшена
```bash
gulp html:dist
```

**Особенности:**
- Поддержка file-include для частичных файлов
- Форматирование HTML (beautify)
- Использование `@@include` для подключения частичных файлов

### Служебные задачи

#### `clean:dev`

Очистка папки `dev/`:
```bash
gulp clean:dev
```

#### `clean:dist`

Очистка папки `dist/`:
```bash
gulp clean:dist
```

#### `cache:clear`

Очистка кэша изображений:
```bash
gulp cache:clear
```

#### `watch`

Слежение за изменениями файлов:
```bash
gulp watch
```

**Отслеживаемые файлы:**
- SCSS файлы
- JavaScript файлы
- Изображения
- Шрифты
- Медиа файлы
- PHP файлы
- HTML файлы

#### `webserver`

Запуск BrowserSync сервера:
```bash
gulp webserver
```

## 👨‍👩‍👧 Работа с дочерними темами

### Автоматическое определение дочерней темы

Gulp автоматически определяет, работаете ли вы в дочерней теме, проверяя наличие `Template: codeweber` в `style.css`.

### Как это работает

1. **Определение темы:**
   - Gulp проверяет `style.css` в текущей директории
   - Если находит `Template: codeweber`, считает тему дочерней

2. **Пути к файлам:**
   - Если `src/` отсутствует в дочерней теме, используется `src/` из родительской темы
   - `dist/` всегда создается в текущей теме (дочерней или родительской)

3. **Логирование:**
   - При запуске Gulp выводит информацию о типе темы
   - Показывает пути к родительской и дочерней темам

### Пример работы в дочерней теме

```bash
# В дочерней теме
cd wp-content/themes/my-child-theme
gulp build:dist
```

**Вывод:**
```
✓ Дочерняя тема обнаружена
  Текущая тема: my-child-theme
  Родительская тема: codeweber
  dist будет создан в: /path/to/my-child-theme/dist
```

### Настройка дочерней темы

1. **Создайте дочернюю тему:**
   ```css
   /*
   Theme Name: My Child Theme
   Template: codeweber
   */
   ```

2. **Скопируйте gulpfile.js:**
   ```bash
   cp wp-content/themes/codeweber/gulpfile.js wp-content/themes/my-child-theme/
   ```

3. **Скопируйте package.json:**
   ```bash
   cp wp-content/themes/codeweber/package.json wp-content/themes/my-child-theme/
   ```

4. **Установите зависимости:**
   ```bash
   cd wp-content/themes/my-child-theme
   npm install
   ```

5. **Запустите сборку:**
   ```bash
   gulp build:dist
   ```

### Переопределение файлов в дочерней теме

- Если `src/` существует в дочерней теме, используются файлы из дочерней темы
- Если `src/` отсутствует, используются файлы из родительской темы
- `dist/` всегда создается в дочерней теме

## 🚀 Команды

### NPM скрипты

В `package.json` определены удобные команды:

```json
{
  "scripts": {
    "start": "gulp serve",
    "build": "gulp build:dist"
  }
}
```

**Использование:**

```bash
# Запуск сервера разработки
npm start

# Сборка для продакшена
npm run build
```

### Основные команды Gulp

```bash
# Полная сборка для продакшена
gulp build:dist

# Сборка для разработки
gulp build:dev

# Запуск сервера разработки
gulp serve

# Слежение за изменениями
gulp watch

# Очистка папок
gulp clean:dev
gulp clean:dist

# Очистка кэша
gulp cache:clear
```

### Команды для отдельных задач

```bash
# CSS
gulp css:dev
gulp css:dist

# JavaScript
gulp themejs:dev
gulp themejs:dist
gulp pluginsjs:dev
gulp pluginsjs:dist

# Изображения
gulp image:dev
gulp image:dist

# Шрифты
gulp fonts:dev
gulp fonts:dist
gulp fontcss:dev
gulp fontcss:dist

# Цвета
gulp colorcss:dev
gulp colorcss:dist

# Vendor
gulp vendorcss:dev
gulp vendorcss:dist
```

## ⚙️ Настройки и конфигурация

### Пути к файлам

Все пути определены в объекте `path` в `gulpfile.js`:

```javascript
var path = {
  dev: {
    // Пути для разработки
    js: "dev/assets/js/",
    css: "dev/assets/css/",
    img: "dev/assets/img/",
    // ...
  },
  dist: {
    // Пути для продакшена
    js: "dist/assets/js/",
    css: "dist/assets/css/",
    img: "dist/assets/img/",
    // ...
  },
  src: {
    // Пути к исходным файлам
    js: "src/assets/js/",
    style: "src/assets/scss/style.scss",
    // ...
  },
  watch: {
    // Пути для слежения
    css: "src/assets/scss/**/*.scss",
    themejs: "src/assets/js/theme.js",
    // ...
  }
};
```

### Настройки BrowserSync

```javascript
var config = {
    server: {
        baseDir: './dist'
    },
    ghostMode: false, // Зеркалирование действий между устройствами
    notify: false     // Уведомления в браузере
};
```

**Настройка для WordPress:**

Если нужно использовать с WordPress, измените настройки:

```javascript
var config = {
    proxy: 'localhost:8080', // URL вашего WordPress сайта
    ghostMode: false,
    notify: false
};
```

### Настройки оптимизации изображений

```javascript
imagemin([
  imagemin.gifsicle({ interlaced: true }),
  jpegrecompress({
    progressive: true,
    max: 90,  // Максимальное качество
    min: 80   // Минимальное качество
  }),
  pngquant(),
  imagemin.svgo({ plugins: [{ removeViewBox: false }] })
])
```

### Настройки SCSS компиляции

- **Автопрефиксер:** Автоматически добавляет вендорные префиксы
- **Unicode поддержка:** `gulp-sass-unicode` для корректной работы с Unicode
- **Source maps:** Создаются для dist сборки
- **Минификация:** `cleanCSS` для dist сборки

### Настройки JavaScript

- **Минификация:** `uglify()` для plugins.js в dist
- **Конкатенация:** `concat()` для объединения vendor файлов
- **Import:** `gulp-js-import` для подключения модулей

## 📦 Обработка файлов

### SCSS → CSS

**Процесс:**
1. Компиляция SCSS в CSS
2. Обработка Unicode символов
3. Добавление автопрефиксов
4. Минификация (для dist)
5. Создание source maps (для dist)

**Файлы:**
- `src/assets/scss/style.scss` → `dist/assets/css/style.css`
- `src/assets/scss/fonts/*.scss` → `dist/assets/css/fonts/*.css`
- `src/assets/scss/colors/*.scss` → `dist/assets/css/colors/*.css`

### JavaScript

**Процесс:**
1. Копирование или сборка файлов
2. Конкатенация vendor файлов
3. Минификация (для dist, если включено)

**Файлы:**
- `src/assets/js/theme.js` → `dist/assets/js/theme.js`
- `src/assets/js/vendor/*.js` + Bootstrap → `dist/assets/js/plugins.js`
- `src/assets/js/restapi.js` → `dist/assets/js/restapi.js`
- И другие модули

### Изображения

**Процесс:**
1. Оптимизация JPEG (качество 80-90%)
2. Оптимизация PNG (pngquant)
3. Оптимизация GIF (gifsicle)
4. Оптимизация SVG (svgo)

**Файлы:**
- `src/assets/img/**/*.*` → `dist/assets/img/**/*.*`

**Кэширование:**
- Используется `gulp-cache` для ускорения повторной обработки
- Очистка: `gulp cache:clear`

### Шрифты

**Процесс:**
1. Копирование файлов шрифтов
2. Компиляция SCSS стилей шрифтов

**Файлы:**
- `src/assets/fonts/**/*.*` → `dist/assets/fonts/**/*.*`
- `src/assets/scss/fonts/*.scss` → `dist/assets/css/fonts/*.css`

### HTML (если раскомментировано)

**Процесс:**
1. Обработка file-include (`@@include`)
2. Форматирование HTML (beautify)

**Использование частичных файлов:**
```html
<!-- В src/index.html -->
@@include('partials/_navbar.html')
@@include('partials/_footer.html')
```

## 🔍 Отладка и решение проблем

### Проблемы с установкой

**Ошибка: "gulp: command not found"**
```bash
npm install -g gulp-cli
```

**Ошибка: "Cannot find module"**
```bash
# Удалите node_modules и установите заново
rm -rf node_modules
npm install
```

### Проблемы с компиляцией SCSS

**Ошибка компиляции SCSS:**
- Проверьте синтаксис SCSS файлов
- Убедитесь, что все импорты корректны
- Проверьте пути к файлам

**Логирование ошибок:**
```javascript
.pipe(sass()
  .on('error', function (err) {
    sass.logError(err);
    this.emit('end');
  })
)
```

### Проблемы с дочерними темами

**Gulp не определяет дочернюю тему:**
- Убедитесь, что в `style.css` есть `Template: codeweber`
- Проверьте, что `gulpfile.js` скопирован в дочернюю тему

**Файлы не находятся:**
- Проверьте, что `src/` существует в родительской теме
- Убедитесь, что пути корректны

### Проблемы с BrowserSync

**Сервер не запускается:**
- Проверьте, что порт 3000 свободен
- Измените порт в настройках, если нужно

**Автоперезагрузка не работает:**
- Убедитесь, что `watch` задача запущена
- Проверьте, что файлы отслеживаются правильно

### Очистка и пересборка

**Полная пересборка:**
```bash
# Очистка кэша
gulp cache:clear

# Очистка dist
gulp clean:dist

# Полная пересборка
gulp build:dist
```

### Проверка версий

```bash
# Версия Node.js
node --version

# Версия npm
npm --version

# Версия Gulp
gulp --version

# Версия установленных пакетов
npm list --depth=0
```

## 📝 Рекомендации

### Для разработки

1. **Используйте `gulp serve`** для разработки с автоперезагрузкой
2. **Работайте в `src/`** - не редактируйте файлы в `dist/`
3. **Используйте source maps** для отладки CSS
4. **Следите за консолью** на ошибки компиляции

### Для продакшена

1. **Всегда используйте `gulp build:dist`** перед деплоем
2. **Проверяйте минифицированные файлы** перед загрузкой
3. **Очищайте кэш** если изображения не обновляются
4. **Проверяйте размеры файлов** после оптимизации

### Оптимизация

1. **Используйте кэш** для ускорения обработки изображений
2. **Очищайте кэш** только при необходимости
3. **Используйте `newer`** для обработки только измененных файлов
4. **Минифицируйте только для продакшена**

## 🔗 Связанные документы

- [IMAGE_CREATION_GUIDE.md](images/IMAGE_CREATION_GUIDE.md) - Работа с изображениями
- [README.md](README.md) - Общая документация темы

## 📚 Полезные ссылки

- [Gulp документация](https://gulpjs.com/docs/en/getting-started/quick-start)
- [Sass документация](https://sass-lang.com/documentation)
- [BrowserSync документация](https://browsersync.io/docs)

---

**Последнее обновление:** 2024-12-13




