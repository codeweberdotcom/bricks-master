# Как использовать формы CodeWeber Forms

После создания формы в CPT `codeweber_form`, вы можете использовать её несколькими способами:

## 📋 Способы использования форм

### 1. Через шорткод (самый простой способ)

Используйте шорткод на любой странице, в посте или в виджете:

```
[codeweber_form id="123"]
```

Где `123` - это **ID формы** (ID поста CPT).

#### Как найти ID формы:

1. Перейдите в **Forms → All Forms**
2. Наведите курсор на название формы
3. Внизу браузера вы увидите ссылку вида: `.../post.php?post=123&action=edit`
4. Число `123` - это и есть ID формы

Или:

1. Откройте форму для редактирования
2. Посмотрите на URL в адресной строке: `.../post.php?post=123&action=edit`
3. Число `123` - это ID формы

#### Примеры использования шорткода:

**Базовое использование:**
```
[codeweber_form id="123"]
```

**С кастомным заголовком:**
```
[codeweber_form id="123" title="Свяжитесь с нами"]
```

**С внутренним именем (для аналитики):**
```
[codeweber_form id="123" name="contact-page-form" title="Форма обратной связи"]
```

---

### 2. Через блок Gutenberg (на страницах/постах) ⭐ РЕКОМЕНДУЕТСЯ

Блок формы доступен на **любых страницах и постах** через редактор Gutenberg:

1. Откройте страницу/пост для редактирования
2. Нажмите кнопку **"+"** (Добавить блок)
3. Найдите блок **"Form"** в категории **"Codeweber Gutenberg Blocks"**
4. Добавьте блок на страницу
5. В настройках блока (панель справа) настройте:
   - **Form ID**: введите ID созданной формы (например, `123`)
     - ID можно найти в списке форм: **Forms → All Forms** (смотрите на URL при редактировании)
     - Или используйте шорткод из колонки "Shortcode" в списке форм
   - **Form Name**: название формы (опционально)
   - **Submit Button Text**: текст кнопки отправки
   - **Submit Button Class**: CSS классы кнопки
   - И другие настройки

**Два способа использования блока:**

**Вариант A: Использовать созданную форму из CPT**
- В поле **Form ID** введите ID формы (например, `123`)
- Форма будет загружена из CPT со всеми полями и настройками
- Это **рекомендуемый способ** - форма управляется централизованно

**Вариант B: Создать форму прямо в блоке (inline)**
- Оставьте **Form ID** пустым
- Добавьте блоки **Form Field** внутри блока формы
- Настройте поля в Inspector Controls
- Форма будет существовать только на этой странице

**Преимущества блока:**
- ✅ Визуальное редактирование
- ✅ Можно настроить внешний вид прямо в редакторе
- ✅ Можно добавить другие блоки рядом с формой
- ✅ Можно использовать одну и ту же форму на разных страницах
- ✅ Централизованное управление формой через CPT

---

### 3. Через PHP код (для разработчиков)

Если вы разрабатываете тему или плагин, можете использовать формы через PHP:

```php
// Способ 1: Через шорткод
echo do_shortcode('[codeweber_form id="123"]');

// Способ 2: Через класс Renderer
if (class_exists('CodeweberFormsRenderer')) {
    $form_post = get_post(123); // ID формы
    if ($form_post && $form_post->post_type === 'codeweber_form') {
        $renderer = new CodeweberFormsRenderer();
        echo $renderer->render($form_post->ID, $form_post);
    }
}

// Способ 3: Через класс Core
if (class_exists('CodeweberFormsCore')) {
    $core = new CodeweberFormsCore();
    $form_config = [
        'id' => 123,
        'name' => 'Contact Form',
        'fields' => [],
        'settings' => [],
    ];
    echo $core->render_form(123, $form_config);
}
```

---

## 🎯 Практические примеры

### Пример 1: Форма на странице "Контакты"

1. Создайте форму в **Forms → Add New Form**
2. Запомните ID формы (например, `123`)
3. Откройте страницу "Контакты" для редактирования
4. Добавьте шорткод: `[codeweber_form id="123"]`
5. Сохраните страницу

### Пример 2: Форма в модальном окне

Если у вас есть модальное окно (например, через `#modal`), вы можете загрузить форму через REST API:

```javascript
// JavaScript пример
fetch('/wp-json/codeweber/v1/form/123')
    .then(response => response.json())
    .then(data => {
        document.getElementById('modal-content').innerHTML = data.html;
    });
```

### Пример 3: Форма в виджете

1. Перейдите в **Внешний вид → Виджеты**
2. Добавьте виджет **"Текст"** или **"HTML"**
3. Вставьте шорткод: `[codeweber_form id="123"]`
4. Сохраните виджет

---

## ⚙️ Настройка формы

После создания формы вы можете настроить её:

### В редакторе формы (Gutenberg):

1. Откройте форму для редактирования
2. Выберите блок **Form**
3. В панели справа (Inspector Controls) настройте:
   - **Form ID**: автоматически (ID поста)
   - **Form Name**: название формы
   - **Submit Button Text**: текст кнопки отправки
   - **Submit Button Class**: CSS классы кнопки
   - И другие настройки

### Через метаполя (в будущем):

В будущих версиях будут доступны метаполя для настройки:
- Email получателя
- Email отправителя
- Тема письма
- Сообщения об успехе/ошибке
- Rate limiting
- Капча

---

## 📧 Просмотр отправок

Все отправки форм сохраняются в базе данных и доступны в админке:

1. Перейдите в **Form Submissions → All Submissions**
2. Вы увидите список всех отправок
3. Можно фильтровать по форме, дате, статусу
4. Можно экспортировать данные

---

## 🔧 Дополнительные возможности

### Переопределение настроек через шорткод:

```
[codeweber_form id="123" title="Новый заголовок" name="custom-name"]
```

### Использование встроенных форм:

Модуль также поддерживает встроенные формы:
- `[codeweber_form id="newsletter"]` - форма подписки на рассылку
- `[codeweber_form id="testimonial"]` - форма отзывов
- `[codeweber_form id="resume"]` - форма резюме
- `[codeweber_form id="callback"]` - форма обратного звонка

---

## ❓ Часто задаваемые вопросы

**Q: Как узнать ID формы?**  
A: Откройте форму для редактирования и посмотрите на URL: `.../post.php?post=123&action=edit` - число `123` это ID.

**Q: Можно ли использовать одну форму на нескольких страницах?**  
A: Да, используйте шорткод `[codeweber_form id="123"]` на любой странице.

**Q: Как изменить внешний вид формы?**  
A: Форма использует стили темы. Вы можете добавить кастомные CSS через **Внешний вид → Настройка → Дополнительный CSS**.

**Q: Где хранятся отправки?**  
A: Все отправки сохраняются в таблице `wp_codeweber_forms_submissions` и доступны в **Form Submissions → All Submissions**.

**Q: Как настроить email уведомления?**  
A: Перейдите в **Form Submissions → Email Templates** и настройте шаблоны писем.

---

## 📚 Дополнительная документация

- [Полная документация модуля](DOCUMENTATION.md)
- [API Reference](API-REFERENCE.md)
- [README](README.md)

